apiVersion: v1
kind: Service
metadata:
  name: {{ include "own-controller.fullname" . }}
  labels:
    {{- include "own-controller.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "own-controller.selectorLabels" . | nindent 4 }}
